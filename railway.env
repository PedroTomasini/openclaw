# OpenClaw - Railway Environment Variables
# Cole estas variáveis em: Railway Dashboard → Your Project → Variables

# Server
PORT=8080
NODE_ENV=production

# Setup Password (obrigatório - para acessar /setup)
SETUP_PASSWORD=CHANGE_ME

# Gateway Auth (opcional - se quiser acessar a API diretamente)
# Gere com: openssl rand -hex 32
# OPENCLAW_GATEWAY_TOKEN=CHANGE_ME

# Diretórios (volume montado em /data)
OPENCLAW_STATE_DIR=/data/.openclaw
OPENCLAW_WORKSPACE_DIR=/data/workspace

# Forçar pnpm
OPENCLAW_PREFER_PNPM=1
